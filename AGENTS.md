# Workflow Rules

## Git Workflow
- Always create a feature/fix branch first
- Push the branch to origin
- Merge to `main` for frontend (TasBon-Bounty-Platform)
- Merge to `dev` for backend (tasbon-api)
- Never commit directly to main/dev without a branch
